/*! 
  SheetJS xlsx.full.min.js vendor slot for SCADA Load Forecast PWA LV5.1

  IMPORTANT:
  This file is a placeholder so the PWA cache and HTML path are stable.
  For full Excel compatibility, replace THIS FILE with the official SheetJS
  standalone browser script named exactly "xlsx.full.min.js".

  Recommended official source:
  https://cdn.sheetjs.com/xlsx-0.20.3/package/dist/xlsx.full.min.js

  When the official file is present, it will define window.XLSX and the
  fallback lite reader (libs/sheetjs-xlsx-lite.js) will not overwrite it.
  If this placeholder is left unchanged, the lite reader will still support
  basic .xlsx/.xlsm files but legacy binary .xls will not be supported.
*/
(function(){
  try { console.warn('Chưa thay libs/xlsx.full.min.js bằng SheetJS full chính thức; dùng fallback lite cho .xlsx/.xlsm cơ bản.'); } catch(e) {}
})();
